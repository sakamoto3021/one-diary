<div class="row">
<% if user_signed_in? %>
  <h1>Tweet一覧</h1>
  <div class="col-sm-4">
    <div class="tweet-form">
      <%= form_with model: @tweet do |f| %>
        <%= f.text_area :content, placeholder: "ここからTweetしてみましょう！", class:'form-controll tweet-form'%>
        <%= f.submit '投稿する', class:'btn btn-block btn-primary'%>
      <% end %>
    </div>

    <div class="serch-form" style="background: red">
      <p>検索フォーム設置予定</p>
    </div>
  </div>

  <div class="col-sm-8">
    <h5>Twitterプレビュー</h5>
    <div class="panel panel-info preview">
      <div class="panel-body">
        <div>
          <img src="<%= current_user.image %>" alt="" class="img-circle pull-left">
          <p class="nickname"><%= current_user.name %><span class="text-muted">@<%= current_user.nickname %></span></p>
          <div id="main">
            main
          </div>
        </div>
      </div>
    </div>
  </div>

  <div id="tweets" class="col-sm-8">
    <%= render 'tweets', tweets: @tweets %>
  </div>

  <% else %>

    <h1>トップページ</h1>
    <p>Twitterを日付ごとに保存</p>
    <p>日記のように読み返せるように</p>
  <% end %>
</div>
<script>
  $(document).ready(function() {
    $("textarea").keydown(function() {
      $("#main").html( $(this).val() );
    });
  });
</script>
